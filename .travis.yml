language: ruby
rvm:
  - 2.2.3
bundler_args: --without production --deployment --jobs=2
cache: bundler
sudo: false
branches:
  only:
    - master
env:
  - DB=mysql
script:
  - bundle exec rake db:drop db:create db:migrate db:seed
  - bundle exec rake spec
notifications:
  email: false
